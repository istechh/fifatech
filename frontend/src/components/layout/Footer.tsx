export function Footer() {
  return (
    <footer className="mt-16 border-t border-[var(--border)] py-8 text-center text-sm text-[var(--text-dim)]">
      <p>
        <strong className="text-[var(--text-secondary)]">ISO Predict</strong> · Prédiction de matches
        internationaux par IA
      </p>
      <p className="mt-1 opacity-50">Données FIFA · Modèle Machine Learning</p>
    </footer>
  );
}
